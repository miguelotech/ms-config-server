server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/miguelotech/config-repo.git
          username: ${GIT_USER}
          password: ${GIT_PASS}
  security:
    user:
      name: root
      password: s3cr3t
